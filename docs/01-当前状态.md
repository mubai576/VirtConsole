# 当前状态

> - 类型：**状态（易变）** —— 动手前先确认新鲜度
> - 权威范围：本文是**项目进度**的唯一来源。规范文件不写进度；结论式历史见 [90-历史记录.md](90-历史记录.md)
> - 最后核实：2026-08-17（对照 commit 44d79c6；`docs/` 重组与 `ui/web/` 其余改动未提交）
> - 相关：[00-索引.md](00-索引.md)

## 一、一句话现状

V1.0 全功能已部署真机并通过 80/80 E2E；V2.0 采集链路（dbus-display 像素路径）真机验证可用；
当前在做 **V3.0 miuix 重构，P0–P4 已完成，只剩 P5 真机回归**。

## 二、版本进度

| 版本 | 目标 | 状态 |
|---|---|---|
| M1 HDMI 单应用渲染 | Weston kiosk + Rust 应用独占 HDMI | ✅ 真机达成，开机自启 |
| V1.0 基础闭环 | PVE 运维 + 终端 + 浏览器 + 模式 1 采集 | ✅ 真机部署，80/80 E2E |
| M2.5 前置 spike | dbus-display + dmabuf 链路验证 | ✅ C1–C5 通过，C6 部分（见 [21](21-采集与输入.md)） |
| V2.0 性能增强 | 60fps 画面（dbus-display 像素路径） | 🔶 采集链路已通，三模自动适配未做 |
| V3.0 miuix 重构 | Vite/React 前端 + miuix 视觉 + 模块整理 | 🔶 P0–P4 ✅，只剩 P5 |
| V3.0 满血版 | SR-IOV / IVSHMEM + 音频 | ⬜ 未开始（且缺硬件，见 §五） |

### V3.0 重构分期

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | Vite/React 骨架 + tokens.css + 构建链 | ✅ |
| P1 | miuix 组件层 + 2K 基准缩放模型 | ✅ |
| P2 | FocusProvider + 4 视图迁移 | ✅ 75/75 |
| P3 | console 层迁移 + 输入链路修复 | ✅ 输入链路已真机验证（VM 9000，2026-08-17） |
| P4 | Rust 模块拆分 + keymap/frame 单测 | ✅ 单测 14→30 |
| P5 | 真机 2K/4K 全链路回归 | ⬜ **待做，V3.0 唯一剩余项** |

## 三、已验证 / 未验证

**已在真机验证**（PVE 9.2 + RTX 5070 Ti，192.168.0.20）

- Weston kiosk 独占 HDMI，两个 systemd 服务开机自启
- PVE 运维闭环：实体列表、详情、启停、快照、监控曲线、宿主终端、内置浏览器
- 模式 1（QMP screendump）采集：720×400 实测 ~1.5ms/帧
- 模式 2（dbus-display）采集：1080p `Scanout` + `Update` 差分推送到 Canvas
- QMP 键鼠投递：Win10 上键盘与鼠标均驱动画面变化
- D-Bus 键鼠（spike 探针直连）：`Keyboard.Press/Release`、`SetAbsPosition` 有效
- **D-Bus 键鼠经 vc-ui 前端链路**（2026-08-17，VM 9000）：`capture_input_ready()` 为真，
  按键全部 `ok===true`，`Scanout 720x400` 有真实帧。此前「前端无效」是部署问题
  （共享总线活在 vc-ui 的 cgroup 里，重启即孤立 QEMU），已修，见 [90](90-历史记录.md)
- 共享 D-Bus 总线独立成 `virtconsole-dbus.service`：停 vc-ui 后总线存活

**尚未验证（重要）**

| 项 | 原因 | 计划 |
|---|---|---|
| **真机 Win10（9200）经 vc-ui 界面的键鼠** | 链路本身已在 **VM 9000** 上真机验证（见下）。9200 的 QEMU 启动于 00:31、早于当前总线（01:34），仍连在旧总线上；且它对 ACPI 关机无响应、没装 guest agent，只能从 Windows 内部重启。**光重启 9200 不够**：`org.qemu` 现由 9000 持有（PID 101233 实测），一条总线只有一个持有者，9200 重启后只会排队、采集仍打到 9000（见 [50-部署运维.md](50-部署运维.md) §3.4） | 顺序固定：先 `qm stop 9000` 让出名字 → 维护者在 Windows 里重启 9200 → 用 `GetConnectionUnixProcessID` 校对持有者确实是 9200 → 复跑 `VIRTCONSOLE_TEST_SUITES=input`。Win10 特有差异在于 scancode 由 guest OS 解释（f173f50 正是 Windows 方向键问题），不能用 9000 的结果代替 |
| **2K/4K 目视观感** | 开发窗口 1280×720 落到兜底档（1rem=8px），只能验结构 | P5 上 HDMI 确认 |
| **WebKitGTK 对 React 19 产物的兼容** | 真机未跑过新前端 | P5；失败则 `vite build --target` 降 es2020 |
| ~~**Linux 侧 unix 分支编译**~~ | ~~交叉编译受阻~~ | ✅ 已在真机验证：首次一编 **22 个错误**（拆分漏改，b2f0153 修好），现零错误零警告，release 21M |
| 4K 采集带宽 | 4K 全帧 base64 约 44MB/帧过 IPC | 见 [21](21-采集与输入.md) §风险 |

## 四、测试与构建现状

| 项 | 数字 | 命令 |
|---|---|---|
| Rust 单测 | 30（vc-ui）+ 7（qmp-engine） | `cargo test --workspace` |
| 前端 E2E | Windows **81/81**；真机 **86/86** | `VIRTCONSOLE_TEST=1 cargo run -p vc-ui` |
| 前端构建 | vite 7.3.6 + React 19 | `npm run build`（在 `ui/web/`） |

两平台差 5 条：`cfg(unix)` 才注册的场景（`capture_start`/`capture_stop`/`capture_status`
相关）。差值曾长期被当成谜，现已对上账。

最新提交 `44d79c6`（总线独立成单元 + install.sh 配套）。
`docs/` 重组与 `ui/web/` 的其余改动仍在工作区，未提交。
模块边界见 [20-架构总览.md](20-架构总览.md)，测试细节见 [40-测试规范.md](40-测试规范.md)。

## 五、已知阻塞与风险

1. **SR-IOV 无硬件**：RTX 5070 Ti 是消费卡，不支持 SR-IOV。V3.0 满血版路线需换硬件。
2. **单 GPU**：显卡直通给 VM 后宿主机将无 GPU（模式 2 与 Weston 合成都需要），需加第二张卡。
3. **NVIDIA 私有 modifier**：`gl=on` 的零拷贝 DMABUF 在外部进程不可 EGL 导入，已改走像素事件路径。详见 [21](21-采集与输入.md)。
4. **旧前端仍在库内**：`ui/frontend/` 保留待回退，P5 确认后删除。注意别改错目录——在用的是 `ui/web/`。
5. **NVIDIA apt 源** 以 `trusted=yes` 绕签名校验；PVE 企业源无订阅时每次 apt 报 401。

## 六、下一步

1. **P5 真机回归**（V3.0 唯一剩余项）：上 HDMI 验 2K/4K 观感 + Win10 键鼠 +
   WebKitGTK 对 React 19 产物的兼容 + unix 分支编译；通过后删 `ui/frontend/`。
   Win10 键鼠一项有前置顺序（停 9000 让出 `org.qemu` → 重启 9200 → 校对持有者），见上表。
2. **V2.0 补齐**：三模自动适配（读 VM 显卡配置选模式）、模式徽标可操作化。
3. 延后项：手机遥控（协议与配对页设计保留）、VM 串口终端、音频重定向。
