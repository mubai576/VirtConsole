# 测试环境访问方式

- 更新时间：2026-08-09
- 用途：VirtConsole 开发联调 / 真机验证（PVE 宿主机）

## SSH 访问

| 项目 | 值 |
| --- | --- |
| 主机 | `192.168.0.20` |
| 端口 | 22（OpenSSH） |
| 用户 | `root` |
| 密码 | `Abc11223.`（注意末尾有句点） |
| 主机名 | `hdmi` |
| 系统 | Debian GNU/Linux 13 (trixie)，内核 `7.0.2-6-pve`（Proxmox VE 9.x） |

## 常用操作

```bash
# 登录
ssh root@192.168.0.20

# 查看 VirtConsole 服务
systemctl status virtconsole-weston virtconsole
journalctl -u virtconsole -f

# 测试模式回归（占用 HDMI，测完自动退出）
systemctl stop virtconsole
XDG_RUNTIME_DIR=/run/virtconsole WAYLAND_DISPLAY=wayland-0 GDK_BACKEND=wayland \
  VIRTCONSOLE_TEST=1 VIRTCONSOLE_TEST_SUITES=terminal /usr/local/bin/vc-ui
echo "exit=$?"
systemctl start virtconsole
```

## 说明

- 密码认证已验证通过（2026-08-09，paramiko 实测连接成功）。
- 真机测试会短暂占用 HDMI 显示，完成后自动退出并恢复服务。
- 详细环境参数（GPU / 驱动 / Weston）见 [里程碑1-真机部署与验证记录.md](里程碑1-真机部署与验证记录.md)。
