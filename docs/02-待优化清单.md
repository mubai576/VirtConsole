# 待优化清单

> - 类型：**状态（易变）** —— 动手前先确认新鲜度
> - 权威范围：本文是**尚未排期的改进意向**的唯一来源。已排期的版本进度在
>   [01-当前状态.md](01-当前状态.md)，本文只放「想做但还没定何时做」的条目。
>   一旦某条进入实施，把它移到 01 的下一步里，本文删除该条。
> - 最后核实：2026-09-21（对照工作树；T4/T5 已落实，T3 探针与 P2 设计已落地）
> - 相关：[00-索引.md](00-索引.md)

## 一、怎么读这份清单

每条都标了**性质**，因为它们的下一步动作完全不同：

| 性质 | 含义 | 下一步 |
|---|---|---|
| 需求 | 要做什么已清楚，缺的是实现 | 直接排期 |
| 设计 | 要做什么本身没定 | 先出设计，再排期 |
| 排查 | 现象已知，原因未知 | 先测量定位，不要先改代码 |

**排查类条目不要跳过测量直接动手。** 本项目已经栽过两次：方向键
scancode 按规律推、Win 键猜是 weston 截走，两次都是「看着像对」的错误
方向，靠真机日志才纠回来。详见 [90-历史记录.md](90-历史记录.md)。

## 二、清单

### 影视与媒体

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T1 | **多声道与 HDR** | 设计 | 影视 Tab 目前只是「开一个独立 Webview 打开固定站点」，音视频能力完全由 WebKitGTK 决定。**设计（2026-09-21）**：真机按清单查能力再定路线——`pactl info`（PipeWire/Pulse 通路）、`weston --version`（color-management 协议支持）、WebKitGTK 测流页（多声道/HDR 是否出）。若 WebKitGTK 给不了，方案是「换播放路径」而非「加配置」。无真机不写代码。 |
| T2 | **音频输出定义与 EQ** | 需求 | 需要选输出设备 + 均衡器。**设计（2026-09-21）**：等 T1 通路结论；若走 PipeWire，EQ 挂 Filter-Chain，输出设备即默认 sink 选择。通路未定前不排期。 |

### 性能

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T3 | **Win 虚拟机采集与鼠标移动性能** | 排查 | **已知**：`mousemove` 默认无节流，每个事件直接发一次 IPC；画面帧走 base64 过 IPC，4K 全帧约 44MB/帧（见 [21](21-采集与输入.md) §风险）。**未知**：卡的是采集侧、IPC 侧还是渲染侧——三者都有嫌疑，必须先分段计时。**别先加节流**。**进展（2026-09-21）**：测量 harness 已落地——前端 `__vcMouseProbe/__vcMouseThrottle`（事件间隔/处理耗时/IPC 计数，节流默认关）、`__vcFrameProbe`（收帧/字节/绘制耗时/丢全帧）、后端 `frame.rs` 编码超 16ms 日志 + 原有每秒 `scanout/update/push/raw MiB/s`。真机用探针读数再定位。 |

### 交互与视觉

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T4 | **整体交互与布局重构** | 设计 | 功能按钮要重新设计，其中有相当一部分是无用的。**先做的事是盘点**：把现有按钮逐个标注「在用 / 无用 / 待定」，无用的删。没有这份盘点，重构会把无用按钮换个样式留下来 |
| T5 | **设置项增删** | 设计 | 加更多可配置项、移除无用项。与 T4 同一个盘点，别拆成两次做 |
| T6 | **屏幕色彩配置** | 需求 | **设计（2026-09-21）**：真色彩管理走 Weston/DRM（CTM/gamma），应用内 CSS 滤镜只影响自家窗口，不做。后者不排期。 |

### 显示与分辨率

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T7 | **分辨率设置 + 高分屏缩放** | 需求 | **设计（2026-09-21）**：分两步。先做只读展示（EDID/ Wayland output 读显示器支持模式 + 当前模式，落系统信息页）；写路径（切分辨率）默认按「需重启 weston」设计应用切模式流程，真机实测若可热切再简化。缩放沿用现有档模型（见 [30](30-UI设计规范.md)）。无真机不写代码。 |
| T8 | **休眠 / HDMI 省电** | 需求 | 长时间无操作后关闭 HDMI 输出，有输入再唤醒。**注意**：唤醒的输入源是本机键鼠；别把它做成「有网络活动就唤醒」，那会永远醒着 |

### Wayland / 合成器

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T9 | **移除 Wayland 默认右键行为，改自定义** | 需求 | 当前右键落到合成器/WebKitGTK 的默认菜单上，不受我们控制 |
| T10 | **Wayland 鼠标移动不跟手** | 排查 | **未知原因**。可能是合成器帧节奏、可能是我们每个 `mousemove` 都发 IPC 反而堵住了（与 T3 同源）。**先测量**：`__vcMouseProbe` 已记录事件间隔与处理耗时，真机读数后再决定改哪边（见 T3 进展）。 |
| T11 | **远程控制时隐藏 Wayland 光标** | 需求 | 现在宿主光标与客户机光标叠加显示，两个指针同屏很怪。远控期间应只留客户机那个 |

### 系统能力

| # | 条目 | 性质 | 已知 / 未知 |
|---|---|---|---|
| T12 | **网络配置与查看** | 需求 | 要能自定义代理与网关。**设计（2026-09-21，无真机未实现）**：代理配置收敛到 `config.json` 一处 `{proxy:{http,https,no_proxy}}`，分别落到 PVE REST（`reqwest` Client 构建期读配置）和内置浏览器（WebView 代理能力待真机 spike，Tauri 侧未必支持按窗口设代理）。网关/查看走只读展示先行。**真机待查**：WebKitGTK 是否跟随环境变量代理。 |
| T13 | **网页自定义 hook（注入 JS）** | 需求 | **设计（2026-09-21，无真机未实现）**：脚本按精确域名白名单存放（`hooks/{domain}.js`，一域一文件一开关），不支持任意 URL 与正则；运行在页面上下文，**不给 IPC 桥**（`invoke` 不可达），只做 DOM 自动化；启用/禁用与脚本内容变更都要落盘审计。先定这三条再排期实现。 |

## 三、条目之间的依赖

不是 13 件独立的事，有几组必须同批考虑：

1. **T4 + T5 是一件事**：按钮盘点与设置项盘点共用同一份「什么在用」的结论。
2. **T1 → T2**：音频通路的结论决定 EQ 挂在哪。
3. **T3 ↔ T10**：都可能是「每个 mousemove 一次 IPC」这一个原因，一次测量能同时回答。
4. **T6 与 T7**：色彩与分辨率都要碰 DRM/Wayland output，同一层的活。

## 附、T4+T5 盘点与落实（盘点 2026-09-21，重构同日落地）

`Ops.jsx` / `Settings.jsx` 头部有 E2E 下标契约，改按钮同步改了套件（C12/C13/C15/H1/H3 下标 1/4/5 未动，G10 由行 6 改为行 5）。

### 按钮（T4）

| 位置 | 按钮 | 结论 | 依据 |
|---|---|---|---|
| 首页快捷 | VM 控制台（动态目标，运行中优先） | ✅ 已落实 | 原写死 9000；mock 下仍选中 9000，I/L7 套件行为不变 |
| 首页快捷 | 内置浏览器 / 设置 | 在用 | 跳转对应 Tab |
| VM 列表 | 刷新 | 在用 | `EntityList.jsx` |
| VM 概览 | 进入采集 / 进入控制台 | 在用 | `views/vm/Overview.jsx`（模式 2 只起采集不进层，模式 1 直接进层，模式 3 无按钮；双语义见 `Vm.jsx enterCapture` 注释） |
| Ops | 进入控制台（自动选路）/ 优雅关机 / 重启 / 启动 / 强制停止 / 新建快照 | 在用 | 快照/危险动作有两级确认；`console` 按 `capture_dbus` 自动进对应链路 |
| Ops | dbus 启用/禁用二选一 | ✅ 已落实 | 原两个常驻按钮，按 `VmDetail.dbus_enabled`（读 VM `args`）只显示其一；0–5 下标不变 |
| 快照行 | 回滚 / 删除 | 在用 | 两级确认 |
| 浏览器 | 打开 / 标签聚焦 / 退格关闭 | 在用 | — |
| 浏览器快速链接 | PVE 后台 / 局域网网关 | 在用 | 内网常用 |
| 浏览器快速链接 | ~~VM 9000 网页控制台~~ / ~~示例站~~ | ✅ 已删 | 前者改走实体页，后者 `example.com` 占位 |
| 影视 | 打开/回到影视窗口 / 关闭影视窗口 | 在用 | `views/Media.jsx`，单窗口复用（测试态自动打开有 `testChecked` 竞态守卫） |
| 实体页 | ◀ 返回 | 在用 | 回列表 |

### 设置项（T5）

| 行 | 结论 | 依据 |
|---|---|---|
| 主题 / 显示缩放 / 画面采集 / PVE 连接 / 开机直连 / 系统信息 | 在用 | 均有 IPC 落点 |
| ~~手机遥控~~ / ~~关于~~ | ✅ 已删 | 前者仅 toast 占位（协议设计保留见 PRD §3.6），后者与系统信息重复 |

## 四、本文不含的内容

已在 [01-当前状态.md](01-当前状态.md) §六 排期的事项（P5 真机回归、V2.0
三模自动适配、延后项手机遥控 / VM 串口终端 / 音频重定向）不重复列在这里。
硬件阻塞（SR-IOV、单 GPU）见 01 §五。
